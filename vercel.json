{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/bar", "destination": "/bar.html" }
  ],
  "functions": {
    "api/catalog/[id].js": {
      "includeFiles": "server/cache/**"
    }
  }
}
